-*- text -*-

*: to do; +: done; @: ongoing; #: blocked; -: rejected

 * More tests, moar! Actually check for test coverage of the code,
   and add missing cases. | Actually, a first check using the
   "coverage" tool shows the situation isn't really bad to begin
   with.

 + describe "default value is callable" trick

 + `schmooze.py -sssn 2 --debug=me --debug and -d you blubber` arg
   passing bug


